package 할일목록;

public class ToDo {
    public static void main(String[] args) {
      String[] todo_list = {"problemsolving", "practiceguitar", "swim", "studygraph"};
      String[] finished = {"true", "false", "true", "false"};
      String[] result = new String[todo_list.length];
      for (int i = 0; i < todo_list.length; i++) {
          if(finished[i].equals("false"));
      }
    }
}
